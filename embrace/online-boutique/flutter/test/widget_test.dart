// Smoke placeholder: full app loads env from assets; integration tests can target a device run.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
